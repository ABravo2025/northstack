import type { ActivityFieldConfigMap } from '../activityLogService.js';

export const noteActivityFieldConfig: ActivityFieldConfigMap = {
  title: { label: 'Title' },
  description: { label: 'Description' },
};
