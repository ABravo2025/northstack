# Notas de avance — semana del 2026-07-21

- 2026-07-21 (Bloque 1 del brief de seguridad — completo, ver `docs/tareas/brief-semana-2026-07-21.md`): el "project manager" (el usuario, en modo planning) armó un brief técnico explícito de 4 bloques para la semana. Se ejecutó el Bloque 1 completo (seguridad, sin dependencias de otros bloques), 5 ítems, cada uno verificado en vivo contra la base productiva de Neon antes de pasar al siguiente, no solo con `npm test`.
  - **1.1 — Mass assignment / IDOR en PATCH employees y clients**: `updateEmployee`/`updateClient` (`employeeService.ts`/`clientService.ts`) pasaban `req.body` crudo a `prisma.*.update({data: input})` — cualquier campo extra (ej. `tenantId`) viajaba intacto hasta la DB. Se reconstruyó `data` explícitamente campo por campo en ambos servicios (mismo patrón whitelist que ya usaba `createEmployee`), y se agregó validación de que `statusId` pertenezca al tenant en `app.ts` (mismo patrón que ya existía para `managerId`). Verificado con un script que registra 2 tenants, intenta reasignar un empleado/cliente al tenant ajeno vía `tenantId` en el body (se ignora, no rompe), intenta aplicar un `statusId` de otro tenant (400), y confirma que los flujos legítimos (nombre, status válido, asignar/desasignar manager) siguen andando igual.
  - **1.2 — Rate limiting en rutas de auth**: se reusó `src/lib/rateLimit.ts` (ya existía, usado en el submit de Public Forms) en vez de sumar `express-rate-limit` — se le agregó soporte de ventana/máximo configurable por llamada (antes hardcodeado a 5/60s) y se cambiaron las keys a llevar un prefijo de scope (`login:`, `register:`, `tenant-register:`, `public-form:`) para que no compartan balde entre sí. Login/register/tenant-register ahora usan 5 intentos/15min. Verificado: 7 logins fallidos seguidos desde la misma IP → los primeros 5 dan 401 normal, el 6to y 7mo dan 429; una IP distinta no se ve afectada por el balde de la otra.
  - **1.3 — Helmet**: dependencia nueva, justificada antes de sumarla (middleware in-process, sin llamar a ningún servicio externo) y aprobada por el usuario. Cabeceras por default, con una excepción explícita: `crossOriginResourcePolicy: { policy: 'cross-origin' }` en vez del default `same-origin`, porque esta API se consume cross-origin por diseño (el frontend en dev corre en otro puerto, y las páginas públicas de `/apply` también). Verificado con Playwright real (no solo curl, porque `Cross-Origin-Resource-Policy` lo aplica el navegador, no algo que curl reproduzca): registro cross-origin, carga de Employees, y el flujo completo de Public Forms con el widget de Turnstile (script + intento de iframe cross-origin) — cero requests bloqueados.
  - **1.4 — Expiración de sesiones (deslizante) + revocar al cambiar password**: el usuario eligió expiración deslizante (se extiende con el uso, en vez de contar fijo desde la creación) antes de programar nada. Schema: `Session.expiresAt` se agregó **nullable primero** (push aditivo), se corrió un script de backfill (`scripts/backfill-session-expiry.ts`, 53 sesiones existentes sembradas con +30 días) y recién después se endureció a NOT NULL (segundo push, ya sin filas nulas). `authenticateToken` ahora rechaza sesiones vencidas y extiende `expiresAt` en cada uso — con una optimización agregada después de la primera ronda de verificación: el `UPDATE` de extensión solo se ejecuta si falta menos de 1 día para que vença (no en cada request), para no duplicar el costo de DB de cada request autenticado del sistema entero. `changeOwnPassword` ahora recibe el token de la sesión actual y borra todas las **otras** sesiones del usuario al cambiar la contraseña (la que la cambió sigue viva). Verificado con 2 "dispositivos" simulados (2 sesiones reales) + cambio de password desde uno: el otro queda 401 de inmediato; sesión con `expiresAt` forzado al pasado da 401; sesión recién creada extiende su `expiresAt` tras un uso.
  - **1.5 — Verificar `user.status` en cada request autenticado**: `authenticateToken` ahora rechaza si `session.user.status !== 'active'`. Verificado: invitar y aceptar un usuario `member`, confirmar que su sesión funciona, desactivarlo desde Company Users como owner, confirmar que la **siguiente** request de esa sesión (sin esperar expiración) da 401 inmediato.
  - **Investigación larga, no bloqueante**: durante la verificación de regresión completa se encontró que un `page.reload()` de Playwright específicamente (no una navegación nueva, no un fetch directo) a veces pierde la sesión con `net::ERR_ABORTED` en el request a `/api/auth/me`. Se aisló exhaustivamente: reproduce igual con Helmet activado y desactivado, en el dev server de Vite y en un build de producción servido con `vite preview`, así que no es causado por ninguno de los 5 cambios del bloque. Abrir una pestaña **nueva** con el mismo token en `localStorage` (navegación real, no reload) restaura la sesión sin problema — apunta a un artefacto específico del método `reload()` de Playwright bajo automatización (ya documentado antes en este proyecto como un patrón de este tipo), no a un bug real de la app. Sin poder confirmarlo 100% sin un F5 real en un browser humano, se lo marca como hallazgo separado a tener en cuenta, no como bloqueante del Bloque 1 — los 5 criterios de aceptación del brief se verificaron todos por API directa, que es la vía autoritativa.
  - **Verificación final**: `npx tsc --noEmit` (backend), `npm run build` (frontend) y `npm test` (backend, 6/6) verdes después de cada tarea individual y otra vez al cierre del bloque completo. Todos los tenants/usuarios de prueba borrados después de cada ronda.
  - **Pendiente antes de Bloque 3**: nada — 1.2 y 1.3 (los prerequisitos que el brief marca para Bloque 3) ya están completos.
- 2026-07-21 (Bloque 2 del brief — checkbox de ToS/Privacy al registrarse, completo): antes de arrancar, 3 preguntas resueltas con el usuario vía opciones (Helmet ya cubierto en Bloque 1; expiración de sesión deslizante; el link de ToS/Privacy apunta a la landing externa, no a contenido nuevo dentro de la app).
  - **2.1 — Checkbox obligatorio + link a Términos/Privacidad**: `User.acceptedTermsAt` nuevo (nullable, push aditivo, sin endurecer — no es retroactivo). `registerUser`/`registerTenantWithOwner` rechazan el registro si `acceptedTerms !== true`. Checkbox agregado en `RegisterPage.tsx` y `AcceptInvitePage.tsx` (este último solo en el modo `register`, no al aceptar una invitación como usuario ya existente). Tests: se actualizó el test de registro existente para pasar `acceptedTerms: true`, y se agregó uno nuevo que confirma el rechazo sin el campo.
  - **Bug encontrado por el usuario después del primer push**: al clickear los links de Términos/Privacidad, error 404 (reportado como "400"). Causa: los links apuntaban a `joinnorthstack.com/terms` y `/privacy` (sin extensión) — el contenido real vive en el sitio estático de la landing, en la branch de git `landing` (separada de `main`, ver commit `92c1966`), servido en `/terms.html` y `/privacy.html` **con** extensión (confirmado inspeccionando `landing:landing/index.html` con `git show`, sin necesidad de hacer checkout de la branch). Corregido en los 2 archivos, verificado con Playwright que el link real ahora carga contenido real (200, no 404).
  - **Pedido de UX del usuario, no en el brief original**: en vez de abrir la landing en una pestaña nueva, mostrar el contenido adentro de un modal sin salir del registro. Se armó un mockup comparativo (Artifact) mostrando "pestaña nueva" vs. "modal in-app" con el costo de cada uno antes de implementar — el usuario eligió el modal. `LegalDocumentModal.tsx` nuevo: hace `fetch` del HTML real de `joinnorthstack.com/terms.html`/`privacy.html` en el momento del click (no duplica el contenido en el repo — así que un cambio futuro en la landing se refleja solo, sin tocar la app), extrae únicamente el `<main>` (parseado con `DOMParser`, dejando afuera el header/footer de la landing), y renderiza eso adentro del modal. Los links cruzados entre Términos↔Privacidad (que en la landing apuntan a `/terms.html`/`/privacy.html`) se interceptan para cambiar de documento **adentro** del mismo modal en vez de navegar afuera. Verificado con Playwright: carga de contenido real, click en el link cruzado cambia de documento sin cerrar el modal, cierre por X/Escape/click en el fondo, y el formulario de registro (campos ya tipeados) queda intacto detrás del modal en todo momento.
- 2026-07-21 (Bloque 3 del brief — notificaciones por email + canal de feedback, completo): las 2 decisiones de producto pendientes (marcadas "a confirmar con el usuario" en el brief) se resolvieron antes de arrancar — `FEEDBACK_EMAIL=info@joinnorthstack.com`, y el caso de auto-aprobación de Time Off (ver 3.2).
  - **3.1 — Email al recibir una submission de Public Form**: `submitPublicForm` (`publicFormService.ts`) ahora, después de crear el Employee/Client, manda un email a todos los `User` con rol `owner`/`admin` del tenant, y una confirmación al que completó el form. Ambos envíos "best-effort" (con `.catch()` que solo loggea) — un fallo de SMTP no puede tirar abajo un submit ya exitoso. Verificado con una llamada directa a `submitPublicForm` (bypaseando Turnstile, que no se puede resolver en un test automatizado) contra SMTP real, sin errores.
  - **3.2 — Email de eventos accionables de Time Off**: `createTimeOffRequest` manda un email al manager asignado cuando la política requiere aprobación (queda `pending`). `decideTimeOffRequest` manda un email al empleado cuando el manager/owner aprueba o rechaza manualmente. **Caso especial confirmado con el usuario**: una política auto-aprobada (`requiresApproval: false`) no tiene a nadie decidiendo activamente — para ese caso, el email de "aprobada" va a **empleado + manager + owner**, no solo al empleado como en la decisión manual (la persona que decide manualmente ya sabe que decidió; en el auto-approve nadie decidió, así que todos necesitan enterarse). Verificado end-to-end con curl: una policy con aprobación → la solicitud queda `pending`; el owner la aprueba → el empleado recibe el email de decisión; una policy auto-approve → la solicitud queda `approved` de inmediato.
  - **3.3 — Canal de feedback/reporte de bugs**: `POST /api/feedback` (autenticado, cualquier rol) arma el mensaje con `nombre + email + tenant + página de origen` y lo manda a `FEEDBACK_EMAIL`. A diferencia de 3.1/3.2, **no** es best-effort — devuelve un error real (502) al usuario si el envío falla, en vez de un 204 falso, porque acá el email en sí es el punto del request, no un efecto secundario de otra acción. Frontend: item "Send feedback" en el dropdown de `TopBar.tsx` (antes solo tenía "Logout") → abre un `SlideOver` con textarea → confirma con toast. Verificado con Playwright: menú → panel → tipear habilita "Send" → toast de éxito → panel se cierra.
  - **Gotcha de deploy**: `FEEDBACK_EMAIL` es una env var nueva — no hay forma de setearla en Vercel por CLI, así que quedó como tarea manual para el usuario (Settings → Environment Variables en el dashboard + un redeploy manual, las env vars no aplican a un deployment ya corriendo). Verificado en 2 pasos: antes de que el usuario la cargara, un test real en producción confirmó 500 ("Feedback is not configured on the server"); después de que la cargó y redeployó, el mismo test dio 204 con el email llegando de verdad.
  - **Investigación no bloqueante, infraestructura de dev local**: durante la verificación de este bloque aparecieron fallos intermitentes (creaciones que tiraban `P2002`/`P2003` en Prisma pero el registro igual quedaba creado una vez). Se aisló la causa: ~25 procesos `tsx watch src/server.ts` huérfanos acumulados a lo largo de la sesión completa (cada reinicio del dev server dejaba el anterior corriendo en vez de matarlo), todos compitiendo por el puerto 3000 y compitiendo entre sí en cada request. Se mataron todos y se estableció el hábito de dejar un solo proceso limpio antes de cada ronda de verificación. No es un bug de la app — apareció solo en el entorno de desarrollo local de esta sesión.
- 2026-07-21 (Bloque 4 del brief — mensaje de agradecimiento personalizable + honeypot en Public Forms, completo): sin preguntas pendientes en el brief para este bloque.
  - **4.1 — Mensaje de agradecimiento personalizable**: `PublicForm.thankYouMessage` nuevo (nullable, push aditivo, sin default — si es `null` el frontend cae al texto genérico de siempre). `createPublicForm`/`updatePublicForm` lo aceptan (recortando espacios, string vacío se guarda como `null`); el endpoint público `GET /api/public/:tenantSlug/:formSlug` lo expone. Frontend: textarea opcional nuevo en el builder de `PublicFormsSettingsPage.tsx`, con el texto default como placeholder; `PublicFormPage.tsx` usa `config.thankYouMessage || 'texto genérico'` en el estado `submitted`. Verificado con curl: form sin mensaje → público expone `null`; form con mensaje → lo expone tal cual; actualizar el form solo por otro campo (ej. el nombre) sin tocar `thankYouMessage` **no lo borra** (chequeo explícito de este caso, porque un `undefined` mal manejado en el `update` de Prisma podría pisarlo con `null` accidentalmente). Verificado también en el navegador con Playwright: se tipea el mensaje en el builder, se guarda, se vuelve a abrir el form para editar y el mensaje sigue ahí.
  - **4.2 — Honeypot anti-spam**: campo `Website` nuevo en `PublicFormPage.tsx`, fuera de la vista real (`position: absolute; left: -9999px`, sin `tabIndex`) — invisible y no alcanzable con Tab para una persona real, pero un bot que completa cualquier campo por nombre/id lo encuentra igual. **Cambio de orden respecto al brief original**: el chequeo del honeypot se puso **antes** de la verificación de Turnstile en `POST /api/public/:tenantSlug/:formSlug/submit` (el brief lo tenía después) — así un bot cae en el honeypot sin gastar una llamada real a la API de Cloudflare, y sin que la ruta llegue siquiera a buscar el form en la base. Si el honeypot llega con valor, la respuesta es 201 igual (no 400), para no darle señal al bot de que fue detectado, pero no se crea ningún registro. Verificado con curl: honeypot completado → 201 sin crear Employee (conteo de filas antes/después, sin cambios) y sin necesitar un `turnstileToken` válido; honeypot vacío con un `turnstileToken` inválido → 400 de Turnstile (confirma que un submit normal sí llega hasta esa verificación, el honeypot no bloquea a usuarios reales).
  - **Pregunta de seguimiento del usuario después de mostrar el resultado**: "¿la página entera de agradecimiento es personalizable?" — aclarado que no, solo el párrafo del mensaje; el heading "Thank you!" y el layout de `PublicFormPage.tsx` en el estado `submitted` siguen fijos. A pedido del usuario, se anotó como idea de backlog (sin implementar, sin spec) más control sobre esa pantalla — heading personalizable, redirect a URL propia del tenant, imagen/logo — ver `docs/tareas-desarrollo.md`.
- 2026-07-22 (Tier 1 — los 5 ítems de `### UX / Interfaz` del recorrido de interfaz, completos): con el brief de la semana ya cerrado, se empezó Tier 1 (definido en la sesión de planning del 07-21) por los 5 ítems de UX pendientes — chicos, mecánicos, con referencia exacta de archivo:línea ya anotada en el backlog, a diferencia de los ítems más grandes de Tier 1 (seed data, FAQ, changelog, métricas) que todavía no tienen ese nivel de spec.
  - **Logo ilegible en dark mode**: antes de tocar código, se verificó contra el CSS real cuál de los 3 archivos señalados en el backlog tenía el bug de verdad. `TopBar.tsx`/`AcceptInvitePage.tsx` sí renderizan sobre `.header`, que va a `dark:bg-gray-950` — ahí el logo navy quedaba casi invisible, corregido con un par de `<img>` (`dark:hidden` / `hidden dark:block`). `AuthLayout.tsx` **no** tenía el bug — su logo vive en `.auth-right`, con un gradiente celeste fijo sin ninguna variante `dark:`, así que el navy ahí siempre fue correcto — el hallazgo original del backlog era un falso positivo para ese archivo específico, se dejó sin tocar y se corrigió la nota en el backlog. El usuario resolvió por su cuenta, en paralelo, el asset `logo-horizontal-dark.svg` en sí (3 tonos claros en vez de blanco plano, Artifact "Northstack — Logo en fondo oscuro").
  - **Company Users al patrón nuevo**: reescritura completa de `CompanyUsersPage.tsx` — `.full-table`, búsqueda, sort por columna (implementación local simple, sin traer la maquinaria de `SavedView`/Views que usan Employees/Clients, porque Users no tiene custom fields ni necesita vistas guardadas), `Pagination.tsx`, y el form "Invite someone" migrado a `SlideOver`. Evaluación pedida por el backlog (íconos sueltos vs. menú de 3 puntos para las acciones de fila): se descartó el menú — el rol ya se edita bien con el `<select>` inline existente, y activar/desactivar es la única acción que necesitaba ícono, así que un solo `.icon-btn` (`LockIcon`/`CheckIcon` según estado) alcanza sin inventar un patrón de menú que no existe en ningún otro lado de la app. La tabla de invitaciones pendientes se migró al mismo patrón.
  - **2 clases de tipografía + `ChevronRightIcon`**: `.page-title`/`.card-title` agregadas a `App.css` (la segunda reemplaza la regla `.card h3` anterior, que no fijaba tamaño explícito); `ChevronRightIcon` agregado a `Icons.tsx` y reemplaza los caracteres `‹`/`›` en `OverviewPage.tsx` (+ `aria-label` nuevo en esos 2 botones, que quedaron solo-ícono).
  - **`docs/design-system.md`**: escrito con las 5 secciones propuestas en el backlog, más una 6ta sobre el logo de marca (distinta de los íconos de UI) documentando la conversión a 3 tonos.
  - **Investigaciones de falsos positivos durante la verificación con Playwright** (ninguna terminó siendo un bug real de la app):
    - Un test que registraba tenants repetidamente pegó contra el rate limiter de auth del Bloque 1 (5 intentos/15min) — el propio rate limiter funcionando como se diseñó, no un bug. Se resolvió reiniciando el dev server (el rate limiter es in-memory) en vez de esperar la ventana.
    - `page.goto()` repetido en el mismo script (en vez de navegación SPA por click) disparó de nuevo el artefacto ya documentado en el Bloque 1 (`net::ERR_ABORTED` en `/api/auth/me` específico de navegaciones completas de Playwright) — se solucionó navegando por click en vez de `page.goto()` después de la carga inicial, igual que haría un usuario real.
    - Un test que abría el SlideOver de invitar y luego mandaba la invitación se quedaba colgado esperando que el panel se cerrara — causa: `navigator.clipboard.writeText()` (ya existente en el código, sin cambios) cuelga indefinidamente en Chromium headless sin `context.grantPermissions(['clipboard-write'])` explícito, porque no hay UI de permiso para descartar en modo headless. En un navegador real, activado por un click real del usuario, el Clipboard API no debería colgarse así. Confirmado otorgando el permiso en el test: funcionó al instante.
  - **Verificación final**: `npm run build` (frontend) y `npm test` (backend, 7/7) verdes. Los 5 ítems verificados en vivo con Playwright (capturas de dark mode, tabla completa de Company Users, SlideOver de invitar, tabla de invitaciones pendientes, sort por columna, búsqueda).
- 2026-07-22 (Tier 2 — arrancado; 2 de 5 ítems completos): el usuario aclaró que "deberían verse muchos más cambios" se refería a que Tier 1 no está terminado (seed data/onboarding, FAQ, changelog, métricas siguen pendientes), no a un bug — confirmado con un screenshot en vivo (fetch sin cache contra producción) de que Company Users sí tiene el rediseño; el problema real era caché del navegador del usuario. Aclarado el malentendido, se pasó a Tier 2 mientras el usuario auditaba Tier 1 por su cuenta.
  - **Validador de dominio de email duplicado**: `registerTenantWithOwner` ahora rechaza un registro nuevo si el dominio del email del owner ya pertenece a un `User` de otro tenant `active` — salvo que el dominio esté en la lista de proveedores genéricos (`GENERIC_EMAIL_DOMAINS`, ~20 dominios tipo gmail/outlook/yahoo, más `example.*` agregado a propósito para no romper los registros de prueba de esta misma sesión). Verificado en vivo, 4 casos: dominio propio nuevo → OK; mismo dominio de nuevo → bloqueado con mensaje claro; dos tenants con `@gmail.com` → ambos OK; tenant original pasado a `suspended` → el dominio deja de bloquear.
  - **Campos nuevos en Sign Up + confirmar contraseña**: `Tenant` ganó `companySize`/`industry`/`country`/`acquisitionChannel` (los 4 nullable, push aditivo; el último es un enum nuevo `AcquisitionChannel` con los mismos 7 valores ya spec-eados en `docs/metrics/saas-metrics-spec.md`, nunca conectados al form real hasta ahora). Los 4 son opcionales (label con "(optional)" explícito) para no meter fricción al registro. `country` usa un `<select>` con ~190 países (`frontend/src/lib/countries.ts`, archivo nuevo). `register-confirmPassword` nuevo, bloquea el submit con un error inline si no coincide con la contraseña. **Decisión de scope**: el ítem original pedía redirigir post-registro a una pantalla de suscripción — no se implementó, esa pantalla es de Tier 4 (Payments/Subscriptions) y todavía no existe; el registro sigue cayendo en `/overview` como hoy. Tampoco se tocó `AcceptInvitePage.tsx` (ese flujo une a alguien a un tenant ya existente, los campos de empresa no aplican ahí). Verificado con Playwright: los 5 campos existen, un mismatch de password bloquea el submit, y un registro exitoso guarda los 4 campos de empresa tal cual se cargaron (confirmado leyendo la respuesta real de la API).
  - **Gotcha recurrente de esta ronda**: varias verificaciones con Playwright pegaron contra el rate limiter de registro (5/15min, del Bloque 1) simplemente por el volumen de tenants de prueba creados — un 429 no loggea nada en el server (a diferencia de un 500), así que el diagnóstico fue confirmar el código de respuesta real en vez de asumir que era un bug nuevo. Mismo fix de siempre: reiniciar el dev server para vaciar el rate limiter in-memory en vez de esperar la ventana.
  - **Pendiente dentro de Tier 2**: campos nuevos en Employee (job title como catálogo, hourly/monthly rate con visibilidad restringida a owner, fechas de ingreso/fin, contract URL, personal email), catálogo de Department (mismo mecanismo, evaluar generalizar en un solo mecanismo reusable en vez de triplicar Status/Department/Job Title), e import/export por CSV genérico — los 3 quedan para la próxima ronda, son más grandes y el catálogo generalizado en particular es una decisión de arquitectura que conviene pensar antes de escribir código.
- 2026-07-22 (Tier 2 — campos nuevos en Employee + catálogo de Department, hechos juntos, completo): antes de escribir código se resolvieron 2 decisiones con el usuario.
  - **Decisión de arquitectura — alcance de la generalización del catálogo**: se planteó explícitamente el tradeoff (unificar Status + Department + Job Title en un solo mecanismo vs. solo Department + Job Title, dejando Status como está) antes de tocar nada — Status es una feature viva con extras que los otros dos no necesitan (agrupamiento de Kanban, `StatusHistoryEntry`, guardrail del default, sembrado en cada alta de tenant), migrarla era riesgo real de regresión por una ganancia de mantenibilidad, no un bug. El usuario confirmó: **solo Department + Job Title comparten mecanismo, Status queda intacto**. Se construyó `FieldCatalogDefinition` (tenant + `kind: 'department' | 'jobTitle'` + name + order + isActive) + `fieldCatalogService.ts` + `/api/field-catalog` + `FieldCatalogMenu.tsx` (componente genérico, mismo patrón visual que `StatusColumnMenu.tsx` sin color/isDefault).
  - **Decisión de producto — Public Forms + Department**: al pasar Department de texto libre a catálogo, un desconocido llenando un formulario público ya no puede escribir cualquier valor nuevo. Confirmado con el usuario: el form público muestra un `<select>` con las opciones activas del catálogo (mismo patrón que un custom field `select`); si el tenant no cargó ningún departamento todavía, el campo se omite del form en vez de mostrar un dropdown vacío. `GET /api/public/:tenantSlug/:formSlug` expone `departmentOptions`; `submitPublicForm` valida el id contra el tenant/estado activo antes de aceptarlo, lo ignora silenciosamente si no matchea (no rechaza el submit entero).
  - **Migración de `Employee.department` (texto libre, obligatorio) a `departmentId` (FK, opcional)**: mismo patrón de migración segura ya usado en el proyecto — `departmentId` nullable primero (push aditivo), backfill (`scripts/backfill-department-catalog.ts`: agrupa por tenant, crea una entrada de catálogo por string distinto — 47 entradas para 32 tenants con datos de prueba existentes — y linkea los 48 empleados; verificado 1:1 contra los valores originales antes de continuar), recién ahí se borró la columna vieja (`prisma db push --accept-data-loss`, seguro porque ya no quedaba nada sin migrar). `departmentId` quedó opcional a propósito (a diferencia del `department` viejo) — evita inventar un valor placeholder para casos sin departamento; el owner auto-creado al registrar un tenant nuevo ya no recibe `department: 'Leadership'` hardcodeado.
  - **Resto de los campos nuevos de Employee**: `jobTitleId` (mismo mecanismo que Department, sin necesidad de migración — campo nuevo). `hourlyRateCents`/`monthlyRateCents` (`Int`, centavos — sigue la convención de `docs/metrics/saas-metrics-spec.md` de nunca usar `Float` para plata); visibilidad solo-owner en `listEmployees` (se borran del JSON si `viewerRole !== 'owner'`) **más allá de lo pedido por el backlog** también se bloqueó la escritura — un `admin` que intente setear estos campos por API recibe 403, para no habilitar un sobrescribir-a-ciegas de un valor que no puede ver. `startDate`/`endDate`/`contractUrl` sin comportamiento automático (informativos únicamente, nada especulativo). `personalEmail` nuevo, sin unicidad; el `email` original no cambió de dato, solo se renombró la etiqueta visible a "Business Email".
  - **Efecto colateral encontrado con un barrido explícito (`grep -rn "\.department\b"` sobre todo el frontend)**: migrar `department` rompía silenciosamente 3 lugares más en `TimeOffOverviewPage.tsx` que también leían `emp.department` directo (uno de ellos con `.toLowerCase()`, que hubiera tirado una excepción real en runtime) — corregidos los 4 puntos encontrados (3 en `TimeOffOverviewPage.tsx` + el ya conocido en `EmployeesPage.tsx`) antes de dar la migración por terminada.
  - **Verificación en vivo**: catálogos de Department/Job Title creados vía API y confirmados apareciendo en los selects reales del form de alta/edición (Playwright, con un `waitForTimeout` explícito después de la primera vez que un timing de carga en paralelo dio un falso negativo); empleado creado con los 10 campos nuevos, todos persistidos correctamente; owner ve compensación completa, admin invitado real no la ve en `listEmployees` y recibe 403 al intentar setearla por API; formulario público confirmado renderizando Department como `<select>` poblado con el catálogo real del tenant. `npm run build`/`npm test` (backend, 7/7) y `npm run build` (frontend) verdes en cada paso.
  - **Gotchas de esta ronda, ninguno terminó siendo bug real**: (1) el mismo rate limiter de registro de rondas anteriores, resuelto igual (reiniciar el dev server); (2) un "no aparecen las opciones del catálogo en el select" que resultó ser el test chequeando el DOM antes de que la carga en paralelo de `loadEmployeeDepartments` terminara — confirmado agregando un `waitForTimeout` y reproduciendo limpio; (3) varios diagnósticos de TypeScript marcados como error en el editor (VS Code) que en realidad eran caché del language server desactualizado después de cada `prisma generate` — confirmado repetidas veces corriendo el `tsc` real (`npm run build`), que siempre dio limpio cuando el editor todavía mostraba error.
- 2026-07-22 (columnas redimensionables por drag en Employees/Clients/Company Users, completo): no era un ítem del backlog — surgió de un reporte en vivo del usuario sobre las tablas ("las columnas no tienen auto fit y no se pueden expandir/contraer a voluntad"). Antes de escribir código se confirmó con el usuario qué pedía en concreto: resize real arrastrando el borde de la columna (estilo Excel/Airtable) en vez de solo un ajuste automático más inteligente del ancho — la opción elegida explícitamente fue "Draggable resize handles".
  - **Mecanismo reusable, pensado para las 3 tablas desde el principio**: hook `useResizableColumns(storageKey)` (`frontend/src/hooks/useResizableColumns.ts`) — trackea el drag con refs (no re-renderiza en cada `mousemove`, solo al soltar/actualizar el ancho), un único listener de `mousemove`/`mouseup` a nivel de `document` registrado una vez por instancia del hook (no uno nuevo por cada columna). Componente `ColumnResizeHandle.tsx` — una tira fina absolutamente posicionada en el borde derecho de cada `<th>`, con `stopPropagation` en su `onClick` para que soltar el mouse sobre el handle sin arrastrar no dispare el sort de esa columna (que ya usa `onClick` en el `<th>`). Anchos persistidos en `localStorage`, una key por tabla (`northstack:columnWidths:employee`/`client`/`companyUser`/`companyUserInvite`) — mismo criterio ya usado en el proyecto para la vista activa guardada y los swatches custom del `ColorPicker`, no se armó modelo de backend para esto.
  - **CSS**: `.full-table` pasó de layout automático a `table-layout: fixed` (necesario para que los anchos de columna se respeten), con un `<colgroup>` nuevo por tabla (un `<col style={{width}}>` por columna, alimentado por el hook) y las celdas cambiaron de wrappear texto a truncar con `overflow-hidden text-ellipsis whitespace-nowrap` — con ancho fijo, el contenido largo necesita truncar en vez de estirar la fila.
  - **Aplicado a las 3 tablas que lo necesitaban**: Employees (la primera, sirvió de referencia para las otras dos), Clients, y Company Users — incluyendo ahí la tabla chica de invitaciones pendientes, que vive en la misma página con su propio storage key separado.
  - **Distinto de un ítem ya anotado en el backlog**: no confundir con "Filtro de columnas visibles en las tablas" (Tier 5) — esa es mostrar/ocultar columnas enteras, esto es cambiar el ancho de una columna visible. Son dos features separadas, sin overlap de código.
  - **Verificación en vivo con Playwright, las 3 tablas**: arrastrar el handle agranda la columna (medido con `boundingBox()` antes/después); el ancho nuevo queda en `localStorage` y sobrevive un `page.reload()`; un click sobre el handle sin arrastrar no cambia el estado de sort de la columna (confirma que el `stopPropagation` funciona). `npm run build` (frontend) verde en cada paso.
- 2026-07-22 (bug real post-push: divisor del resize casi invisible, corregido): después de pushear y confirmar en producción con Playwright que el feature funcionaba, el usuario reportó "no cambió nada, ni en local ni en producción" con una captura real de su navegador. Diagnóstico: el deploy sí se había completado (confirmado con el bundle de producción y el timestamp `Last-Modified`), y el drag funcionaba — pero el usuario no lo veía porque el color del divisor (`.col-resize-handle`, borde de 1px) tenía contraste insuficiente contra el fondo. Calculado el contraste real: `gray-300` sobre crema (modo claro) daba 1.44:1, `gray-600` sobre `gray-950` (modo oscuro) daba 2.66:1 — ambos por debajo del mínimo WCAG de 3:1 para componentes de UI no textuales. Las capturas de verificación anteriores habían usado `deviceScaleFactor: 4-6` (zoom/supersampling) para poder ver el detalle, lo cual ocultó que a resolución 1x real (la que ve un usuario) el divisor era casi imperceptible. Corregido a un solo color, `gray-500` (contraste ~4.2:1 oscuro / ~4.7:1 claro, sin necesitar variante por tema), re-verificado con capturas a `deviceScaleFactor: 1` explícito esta vez. **Lección para futuras verificaciones de UI sutil**: cuando el elemento a verificar es un detalle visual fino (líneas, bordes de 1px), tomar al menos una captura a resolución 1x además de cualquier zoom, y idealmente calcular el contraste real contra el fondo en vez de confiar solo en la inspección visual de la captura.
- 2026-07-22 (rediseño de Employees/Clients/Company Users para igualar el mockup ya aprobado, completo): tras la confirmación de que el resize sí estaba en producción, el usuario compartió una captura de su tabla real de Employees en producción — visualmente muy distinta del Artifact "Northstack — Diseño de pantallas" (el mismo mockup usado como referencia aprobada para el rediseño de Company Users en Tier 1, 2026-07-21) y dijo: "el módulo se ve totalmente distinto, deberíamos respetar la misma estructura en toda la app". Se releyó el artifact completo (vía `WebFetch` sobre la URL que el usuario había compartido) para confirmar el gap exacto contra el HTML/CSS real del mockup, no solo contra la captura.
  - **Gap identificado**: el mockup ya especificaba avatares circulares con iniciales junto al nombre, Status como punto de color + texto (en vez de texto plano), Role como pill de color (verde/azul/gris según owner/admin/member), y encabezados de tabla en mayúsculas chicas con letter-spacing — pero la implementación real de las 3 tablas (Employees/Clients/Company Users) nunca había llegado a aplicar ese lenguaje visual, se había quedado en texto plano en cada celda.
  - **3 componentes nuevos, reusables entre las 3 tablas**: `Avatar.tsx` (iniciales de nombre+apellido, círculo `brand-blue-light`/texto `brand-navy`, **mismo color en los 2 temas** — así lo define el mockup, sin variante `dark:`), `StatusChip.tsx` (punto de color + label — en Employees/Clients usa el `color` real ya guardado en cada `StatusDefinition`, el mismo que el usuario configura vía "Manage options"; en Company Users, que no tiene `StatusDefinition`, usa colores fijos verde/gris para activo/inactivo), `RoleChip.tsx` (pill de color, solo Company Users, mapea `owner`→verde/`admin`→azul de marca/`member`→gris).
  - **Decisión de scope en Company Users**: el mockup solo mostraba el estado estático (chip), no resolvía cómo editar el rol en línea. Se mantuvo el `<select>` nativo existente para las filas donde el usuario actual puede cambiar el rol (`canEditRole === true`) y se usa `RoleChip` solo en las filas no editables — prioridad a no perder funcionalidad real de edición sobre calzar 100% con el mockup estático.
  - **Tipografía de encabezado**: `.full-table th` pasó de `text-sm font-semibold text-brand-navy` a `text-xs font-bold uppercase tracking-wide text-gray-500`, **escopeado únicamente a `.full-table`** (no se tocó `.table th` de base) para no afectar otras tablas más simples de la app (ej. PTO Policies) que no estaban en el alcance del mockup ni de este reporte.
  - **El Kanban de Employees no lleva avatar** — el mockup tampoco lo muestra ahí, a propósito distinto del grid; no se agregó por consistencia forzada donde el propio mockup no lo pedía.
  - **Verificación**: capturas en vivo (local, light y dark) de las 3 tablas comparadas contra el mockup — avatares, chips y tipografía coinciden. Re-verificado que el resize de columnas (implementado en la ronda anterior) sigue funcionando con la nueva tipografía de encabezado (no es un elemento posicionado por texto, así que no se esperaba romperse, pero se confirmó explícitamente). `npm run build` (frontend) y `npm test` (backend, 7/7) verdes. Documentado en `docs/design-system.md` sección 7 (nueva).
  - **Nota abierta, sin resolver todavía**: al releer el artifact se encontró que el ícono del logo en fondo oscuro (`logo-horizontal-dark.svg`) tiene 2 de sus 3 tonos casi idénticos (`#fdfcf8` y `#ffffff`, contraste ~1.05:1), por lo que en la práctica se ve como blanco plano en vez de mostrar las 3 capas de profundidad que sí se ven en el ícono de fondo claro. Reportado al usuario, no confirmado todavía si es el mismo hallazgo de su UX/UI manager ni aplicado ningún fix — ver nota en `docs/design-system.md` sección 6.
- 2026-07-22 (ronda de feedback sobre las tablas, completo): el usuario probó el rediseño en vivo y mandó una lista densa de 4 pedidos juntos, referenciando una captura del popover "Manage options" de Status. Se armó un todo list de 7 pasos antes de tocar código, dado que tocaba varios componentes compartidos y 3 páginas.
  - **1. Punto de color de Status editable + tamaño completo**: el pedido decía "the grey point must be editable too... should be full size". Verificado contra el código: `StatusColumnMenu.tsx` solo tenía un `<span className="color-dot">` de 10px, decorativo — el `ColorPicker` completo (28px, clickeable) solo aparecía en la sección "Add status" de abajo, para el status *nuevo*. Se cambió el `<span>` de cada fila existente por el mismo `ColorPicker`, con un `handleColorChange` nuevo que llama `updateStatusDefinition(id, { color })`.
  - **2. "if the left panel is open, have to adjust"**: interpretación inicial (probada y descartada): un popover ya abierto no se reposiciona si el sidebar se colapsa/expande *mientras sigue abierto* — se escribió un test para ese escenario exacto y falló por una razón distinta: el click en el botón de colapsar sidebar cuenta como "click afuera" para el popover (comportamiento ya existente, correcto) y lo cierra antes de que import ninguna reposición. Se descartó esa lectura y se probó la otra: el popover *anidado* de `ColorPicker` (el que se abre al clickear el punto de color, dentro del popover de Status) usaba su propio `<div className="absolute">` sin límite de viewport — con el sidebar **expandido** (le resta ancho a la tabla, empuja Status hacia la derecha) ese popover interno se podía salir de pantalla. Confirmado con un test en un viewport angosto (1000px, sidebar expandido): el picker sin fix se hubiera salido; con el fix (`ColorPicker` refactorizado para usar el `Popover.tsx` compartido, que sí clampea contra `window.innerWidth`) el borde derecho quedó dentro del viewport con margen. De paso se mejoró `Popover.tsx` para recalcular posición en cada frame mientras está abierto (antes solo al abrir) — beneficia a todos los popovers de la app contra cualquier corrimiento de layout que no dispare `resize`, no solo este caso.
  - **3. Altura de "Add Employee" vs. buscador y Filter**: medido en el CSS real antes de tocar nada — `.btn-primary` ~40px (padding+line-height), `.toolbar-search` ~38px, `.tb-btn` (Filter) ~30px. Se fijaron los 3 a 40px explícitos con `h-10`, y un modificador nuevo `.btn-toolbar-size` (mismo criterio que `.btn-tab-size`, que ya existía para el mismo problema en `.views-bar`) aplicado solo a los 3 botones "Add X"/"Invite" que viven en un `.page-toolbar` — sin tocar `.btn-primary` de base, usado en decenas de forms/dialogs.
  - **4. Columnas ocultables + Filter solo-ícono**: pedido nuevo que activa una idea ya anotada en Tier 5 del backlog ("Filtro de columnas visibles"), ahora movida a hecha. Hook `useColumnVisibility(storageKey)` (mismo patrón que `useResizableColumns` de la ronda anterior — Set de keys ocultas en `localStorage`, una key por tabla) + `ColumnVisibilityMenu.tsx` (botón `EyeIcon`, popover con checkboxes). El botón Filter perdió su texto ("remove the label filter and just leave the funnel") y pasó a ser un cuadrado de 40×40 solo-ícono, mismo tamaño que el nuevo botón de columnas — los dos quedaron pegados en la toolbar. Aplicado a Employees/Clients/Company Users: cada `<td>`/`<th>`/`<col>` de columna base se envolvió en su chequeo de visibilidad individual (los `<td>` no se generaban con `.map()` como los headers, así que fue campo por campo, no una sola línea).
  - **5. "Hide" faltante en los 3 puntos**: `StatusColumnMenu`/`FieldCatalogMenu`/`CustomFieldColumnMenu` ganaron un prop `onHide?` que agrega "Hide column" arriba del popover cuando se pasa — llama al mismo `hideColumn()` del punto anterior, así ocultar una columna es accesible tanto desde el menú de columnas nuevo como desde el menú de 3 puntos de esa columna puntual.
  - **Gotcha de esta ronda**: dos falsos negativos en los tests propios, no bugs reales — (1) el test de "hide column" en el menú de Status falló porque `Escape` cerraba el popover *entero* de Status, no solo el color picker anidado que se acababa de abrir para probar el punto 2 — ambos escuchan `Escape` independientemente mientras están abiertos; se corrigió el test para cerrar el picker con un click en su propio trigger en vez de `Escape`. (2) el primer test de reposicionamiento de popover asumía un escenario (toggle de sidebar con el popover ya abierto) que en realidad no es alcanzable en la UI real, por la razón del punto 2 arriba — se descartó ese test y se reemplazó por el de viewport angosto, que sí prueba el bug real encontrado.
  - **Verificación final**: las 3 tablas con Playwright — alturas de toolbar iguales (40px) en Employees/Clients/Company Users; ocultar una columna la saca de la tabla y sobrevive un `page.reload()`; el color picker anidado no se sale del viewport en 1000px de ancho con sidebar expandido; "Hide column" presente y funcional en el menú de Status; el resize de columnas (ronda anterior) re-verificado sin regresión. `npm run build` (frontend) y `npm test` (backend, 7/7) verdes.
- 2026-07-23 (2 bugs más sobre el punto de color de Status, encontrados por el usuario probando en producción, corregidos): el fix de la ronda anterior arreglaba el *clamping* de viewport del color picker anidado, pero no probaba el flujo real de seleccionar un color — ahí apareció el bug de verdad.
  - **"se cierra automáticamente, invalidando el cambio de color"**: causa real — `Popover.tsx` decide si un click es "afuera" chequeando `popoverRef.current.contains(target)`, pero el `ColorPicker` anidado ahora también usa `Popover`, que renderiza en su *propio* portal a `document.body` — un click dentro del picker anidado no es descendiente DOM del popover exterior (aunque sí lo sea en el árbol de React), así que el popover de Status lo interpretaba como click afuera y se cerraba antes de que el cambio de color terminara de reflejarse. Corregido en `Popover.tsx`: `handlePointerDown`/`handleScroll` ahora también toleran un click dentro de *cualquier* `.popover-panel`, no solo el propio — cubre cualquier popover anidado futuro, no solo `ColorPicker` dentro de `StatusColumnMenu`. Verificado con Playwright esperando el tiempo real del round-trip (PATCH + refetch, ~2-4s en este entorno — el primer intento del test dio un falso negativo esperando solo 300ms).
  - **Pedido nuevo, no un bug**: reemplazar las flechas ▲/▼ de reordenar por un handle de 6 puntos con drag-and-drop. `GripIcon` ya existía en `Icons.tsx` y ya se usaba para drag-and-drop en `PublicFormsSettingsPage.tsx` — se reusó el mismo patrón HTML5 nativo (`draggable` + `onDragStart`/`onDragOver`/`onDrop`), sin agregar ninguna librería. Aplicado a `StatusColumnMenu` y `FieldCatalogMenu` (Department/Job Title) para no dejar el segundo con el patrón viejo. Verificado con `locator.dragTo()` de Playwright — un intento inicial con `mouse.move/down/up` sintético no disparaba los eventos HTML5 de drag reales y daba falso negativo; `dragTo()` sí los dispara.
  - `npm run build` (frontend) y `npm test` (backend, 7/7) verdes.
