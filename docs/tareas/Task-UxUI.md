# Task-UxUI — entregas del agente UX/UI

Spec visual (mockup/Artifact aprobado + detalle de layout, estados y componentes a reutilizar) para que el agente de Development lo ejecute. Cada entrada lleva fecha de carga. Una vez implementado, Development lo marca en su propia nota de avance (`docs/tareas/semana-*.md`) — este archivo no se edita para tachar ítems, solo para agregar nuevos.

Los ítems de UX/Interfaz ya especificados antes de 2026-07-23 (tablas estilo ClickUp: `.container` de `AppLayout`, vista List, tipografía, panel Overview push-left, ghost row/card de Add, hover de fila completo; y la scrollbar propia) siguen viviendo en `docs/tareas-desarrollo.md`, sección "UX / Interfaz" — no se migraron para no duplicar el punto de referencia que Development ya usa. Este archivo arranca vacío y se usa para todo lo que yo entregue de acá en adelante.
