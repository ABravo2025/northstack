import type { ActivityFieldConfigMap } from '../activityLogService.js';

export const invitationActivityFieldConfig: ActivityFieldConfigMap = {
  email: { label: 'Email' },
  role: { label: 'Role' },
  status: { label: 'Status' },
};
