export default function ClientsDashboardPage() {
  return (
    <div className="card">
      <h3>Clients Dashboard</h3>
      <p className="text-sm text-gray-500">
        Metrics for Clients (pipeline, status breakdown, etc.) are coming in a future project. For
        now, head to Clients to manage your list.
      </p>
    </div>
  );
}
